## Activate Conda Env
`conda activate {full path of ci-proj folder}` or create a new ENV with `conda create -n {env name} python=3.10 -y` then activate

## Install Packages
Install the required packages from `requirements.txt`

## Run Application
`streamlit run app\app.py`